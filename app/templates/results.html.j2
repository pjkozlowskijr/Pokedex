{% extends "base.html.j2" %}

{# {% block css %}

{% endblock %} #}

{% block title %}
Battle
{% endblock %}

{% block content %}

<div id="result_head">
    <h1> Pok&eacute;mon Battleground</h1>
    <hr>
    <a class="btn" href="{{url_for('main.view_users')}}">Battle Again&nbsp;<i class="bi bi-arrow-repeat"></i></a>
</div>
<div id="result_body">
    <div id="outcome">
        <h2>The battle has ended!</h2>
        {% for result in results[-1] %}
            {{result}}
        {% endfor %}
        <img src="{{winner[0].get_icon_url()}}" alt="Winner's avatar.">
    </div>
    <div id="recap">
        <h2>A summary of last round's action:</h3>
        {% for result in results[:-1] %}
            {{result}}
        {% endfor %}
    </div>
</div>

{% endblock %}