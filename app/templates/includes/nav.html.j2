<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <span class="navbar-brand">
        <img src="https://res.cloudinary.com/detcvmtip/image/upload/v1651806710/pokedex_images/pokeball_jooxgb.png" alt="Pokeball" width="30" class="d-inline-block align-text-bottom">
        Pokedex
        </span>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            {% if current_user.is_authenticated %}
                <div class="navbar-nav me-auto">
                    <a class="nav-link" href="{{url_for('main.index')}}">Home&nbsp<i class="bi bi-house-door-fill"></i></a>
                    <a class="nav-link" href="{{url_for('main.lookup')}}">Find Pokemon&nbsp<i class="bi bi-binoculars-fill"></i></a>
                </div>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="{{url_for('auth.edit_profile')}}">Profile&nbsp<img src="{{current_user.get_icon_url()}}" style="height: 26px" class="align-text-bottom"></a>
                    <a class="nav-link" href="{{url_for('auth.logout')}}">Logout&nbsp<i class="bi bi-box-arrow-right"></i></a>
                </div>
            {% else %}
                <div class="navbar-nav me-auto">
                    <a class="nav-link" href="{{url_for('main.index')}}">Home&nbsp<i class="bi bi-house-door-fill"></i></a>
                </div>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="{{url_for('auth.login')}}">Login&nbsp<i class="bi bi-box-arrow-in-right"></i></a>
                    <a class="nav-link" href="{{url_for('auth.register')}}">Register&nbsp<i class="bi bi-clipboard"></i></a>     
                </div>
            {% endif %}
        </div>
    </div>
</nav>